apiVersion: v1
kind: Secret
metadata:
    name: restic-config-data
type: Opaque
stringData:
    RESTIC_PASSWORD: ${RESTIC_PASSWORD}
    RESTIC_REPOSITORY: ${RESTIC_REPOSITORY_BASE}/linkding/data
    AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
    AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
