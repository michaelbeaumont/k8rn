---
apiVersion: v1
kind: Namespace
metadata:
  name: zfs-scrub
  labels:
    pod-security.kubernetes.io/enforce: privileged
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: zfs-scrub
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  - Egress
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: weekly-zfs-data-scrub
  namespace: zfs-scrub
  annotations:
    ignore-check.kube-linter.io/privilege-escalation-container: "it has to be privileged"
    ignore-check.kube-linter.io/privileged-container: "it has to be privileged"
    ignore-check.kube-linter.io/run-as-non-root: "it has to be root"
    ignore-check.kube-linter.io/latest-tag: "it's my image"
spec:
  concurrencyPolicy: Forbid
  jobTemplate:
    metadata:
      name: weekly-zfs-data-scrub
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          affinity:
            nodeAffinity:
              requiredDuringSchedulingIgnoredDuringExecution:
                nodeSelectorTerms:
                  - matchExpressions:
                      - key: kubernetes.io/hostname
                        operator: In
                        values:
                          - k8rn-core
          containers:
            - name: scrub
              image: ghcr.io/michaelbeaumont/zfs-scrub:latest
              imagePullPolicy: Always
              args:
                - data
              securityContext:
                readOnlyRootFilesystem: true
                privileged: true
  schedule: "0 0 * * 1"
